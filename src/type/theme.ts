export declare type Theme = {
    dark: boolean,
    colors: {
        primary: string,
        background: string,
        background_deactive: string,
        card: string,
        text: string,
        border: string,
        notification: string,
    }
}